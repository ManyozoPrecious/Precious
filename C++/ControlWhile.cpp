#include <iostream>
using namespace std;
int main() {
    int count = 1;
    while (count<=10) //condition
    {
        cout<<"Hello there"<<endl; //statements to execute if the condition is true
        count += 1; // same as count = count + 1
    }
    return 0;
}